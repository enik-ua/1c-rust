Процедура ПередНачаломРаботыСистемы(Отказ)
	
КонецПроцедуры

Процедура ПриНачалеРаботыСистемы()
	
	Каталог = ПараметрЗапуска;
	Если Не ЗначениеЗаполнено(НайтиФайлы(Каталог+ "\src")) Тогда
		СоздатьКаталог(Каталог+"\src");
	КонецЕсли;
	Трансляция.ВыполнитьТрансляцию(Каталог);
	КомандаСистемы("rustc main.rs",Каталог+"\src\");
		
КонецПроцедуры

